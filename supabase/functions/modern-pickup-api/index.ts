import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const RELEASE_PRODUCT="ParkingMartin-G";
const RELEASE_VERSION="1.4.0";
const RELEASE_BUILD="2026.09.04.04";
const RELEASE_SOURCE_REVISION="ab10cd20ef30ab40cd50ef60ab70cd80ef90ab01";
function releaseAttestation(){return new Response(JSON.stringify({ok:true,product:RELEASE_PRODUCT,function:"modern-pickup-api",version:RELEASE_VERSION,build:RELEASE_BUILD,source_revision:RELEASE_SOURCE_REVISION}),{headers:{"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store","X-Content-Type-Options":"nosniff"}})}

const BOT_TOKEN=Deno.env.get("TELEGRAM_BOT_TOKEN")!;const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!;const SECRET_KEYS_JSON=Deno.env.get("SUPABASE_SECRET_KEYS");const LEGACY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");const GOOGLE_VISION_KEY=Deno.env.get("GOOGLE_VISION_API_KEY")||Deno.env.get("GOOGLE_API_KEY")||null;const ORIGIN="https://jdlc86.github.io";
const REQUIRED_STATE_SLOTS=['front','front_right','left','right','rear','rear_left','front_left','rear_right','dashboard','odometer'] as const;const REQUIRED_SET=new Set<string>(REQUIRED_STATE_SLOTS);const MAX_EXTRA_STATE_PHOTOS=10;
function key(){if(SECRET_KEYS_JSON){try{const p=JSON.parse(SECRET_KEYS_JSON);if(p?.default)return p.default;const v=Object.values(p??{})[0];if(typeof v==='string')return v}catch{}}if(LEGACY)return LEGACY;throw new Error('no_server_key')}function hdr(x:Record<string,string>={}){const k=key();return{apikey:k,Authorization:`Bearer ${k}`,...x}}function cors(){return{'Access-Control-Allow-Origin':ORIGIN,'Access-Control-Allow-Headers':'content-type','Access-Control-Allow-Methods':'POST,OPTIONS','Vary':'Origin'}}function json(x:any,s=200){return new Response(JSON.stringify(x),{status:s,headers:{'Content-Type':'application/json',...cors()}})}
function eq(a:Uint8Array,b:Uint8Array){if(a.length!==b.length)return false;let x=0;for(let i=0;i<a.length;i++)x|=a[i]^b[i];return x===0}function hb(s:string){if(!/^[0-9a-f]{64}$/i.test(s))return null;const a=new Uint8Array(32);for(let i=0;i<32;i++)a[i]=parseInt(s.slice(i*2,i*2+2),16);return a}async function hmac(k:Uint8Array|string,m:string){const kb=typeof k==='string'?new TextEncoder().encode(k):k,ik=await crypto.subtle.importKey('raw',kb,{name:'HMAC',hash:'SHA-256'},false,['sign']);return new Uint8Array(await crypto.subtle.sign('HMAC',ik,new TextEncoder().encode(m)))}async function auth(d:string){const p=new URLSearchParams(d),hash=p.get('hash')||'';p.delete('hash');const check=[...p.entries()].sort((a,b)=>a[0].localeCompare(b[0])).map(([k,v])=>`${k}=${v}`).join('\n'),sec=await hmac('WebAppData',BOT_TOKEN),calc=await hmac(sec,check),given=hb(hash);if(!given||!eq(calc,given))throw new Error('invalid_init_data');const at=Number(p.get('auth_date')||0);if(!Number.isFinite(at)||Math.abs(Date.now()/1000-at)>600)throw new Error('expired_init_data');const u=JSON.parse(p.get('user')||'null');if(!u?.id)throw new Error('missing_user');return Number(u.id)}
async function rest(t:string,p:Record<string,string>){const u=new URL(`${SUPABASE_URL}/rest/v1/${t}`);for(const[k,v]of Object.entries(p))u.searchParams.set(k,v);const r=await fetch(u,{headers:hdr({Accept:'application/json'})});if(!r.ok)throw new Error(await r.text());return r.json()}async function one(t:string,p:Record<string,string>){return(await rest(t,{...p,limit:'1'}))[0]??null}async function insert(t:string,b:any,rep=true){const r=await fetch(`${SUPABASE_URL}/rest/v1/${t}`,{method:'POST',headers:hdr({'Content-Type':'application/json',Prefer:rep?'return=representation':'return=minimal'}),body:JSON.stringify(b)});if(!r.ok)throw new Error(await r.text());return rep?(await r.json())[0]:null}async function patch(t:string,f:Record<string,string>,b:any){const u=new URL(`${SUPABASE_URL}/rest/v1/${t}`);for(const[k,v]of Object.entries(f))u.searchParams.set(k,v);const r=await fetch(u,{method:'PATCH',headers:hdr({'Content-Type':'application/json',Prefer:'return=minimal'}),body:JSON.stringify(b)});if(!r.ok)throw new Error(await r.text())}async function del(t:string,f:Record<string,string>){const u=new URL(`${SUPABASE_URL}/rest/v1/${t}`);for(const[k,v]of Object.entries(f))u.searchParams.set(k,v);const r=await fetch(u,{method:'DELETE',headers:hdr({Prefer:'return=minimal'})});if(!r.ok)throw new Error(await r.text())}
const norm=(s:string)=>s.toUpperCase().replace(/[^A-Z0-9]/g,'');async function ctx(uid:number){const u=await one('telegram_users',{telegram_user_id:`eq.${uid}`,active:'eq.true',select:'telegram_user_id,role,username,first_name,last_name'});if(!u)throw new Error('not_authorized');let w=await one('workers',{telegram_user_id:`eq.${uid}`,active:'eq.true',select:'id,full_name'});if(!w){const full=[u.first_name,u.last_name].filter(Boolean).join(' ')||(u.username?`@${u.username}`:`Usuario ${uid}`);w=await insert('workers',{telegram_user_id:uid,full_name:full,role:u.role==='owner'||u.role==='admin'?'admin':'operator',active:true})}return{u,w}}async function vehicle(p:string){return one('vehicles',{normalized_plate:`eq.${p}`,select:'id,plate,status'})}
async function rpc(name:string,body:any){const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/${name}`,{method:'POST',headers:hdr({'Content-Type':'application/json'}),body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.message||d?.error||'rpc_failed');return d}
async function cancelActivePickup(wid:string,uid:number){const rows=await rest('operation_flow_sessions',{worker_id:`eq.${wid}`,flow_type:'eq.pickup',status:'eq.active',select:'id'});for(const s of rows)await rpc('cancel_operation_flow_session',{p_session_id:s.id,p_worker_id:wid,p_telegram_user_id:uid,p_flow_type:'pickup'})}
async function createPickupSession(plate:string,v:any,w:any,uid:number,created:boolean){await cancelActivePickup(w.id,uid);return insert('operation_flow_sessions',{flow_type:'pickup',normalized_plate:plate,vehicle_id:v.id,worker_id:w.id,telegram_user_id:uid,vehicle_created_by_session:created,status:'active',current_step:'started'})}
async function requirePickupSession(id:string,plate:string,v:any,w:any,uid:number){if(!id)throw new Error('flow_session_required');const s=await one('operation_flow_sessions',{id:`eq.${id}`,select:'id,flow_type,normalized_plate,vehicle_id,worker_id,telegram_user_id,status,expires_at'});if(!s)throw new Error('flow_session_not_found');if(s.status!=='active')throw new Error('flow_session_not_active');if(new Date(s.expires_at).getTime()<=Date.now())throw new Error('flow_session_expired');if(s.flow_type!=='pickup'||s.normalized_plate!==plate||s.vehicle_id!==v.id||s.worker_id!==w.id||Number(s.telegram_user_id)!==uid)throw new Error('flow_session_context_mismatch');return s}
async function req(type:string){const r=await one('evidence_requirements',{stage:'eq.airport_pickup',evidence_type:`eq.${type}`,active:'eq.true',select:'min_count,max_count'});return{min:Number(r?.min_count??0),max:r?.max_count==null?null:Number(r.max_count)}}async function count(id:string,type:string,sessionId:string){const a=await rest('vehicle_evidence',{vehicle_id:`eq.${id}`,stage:'eq.airport_pickup',evidence_type:`eq.${type}`,event_id:'is.null','metadata->>flow_session_id':`eq.${sessionId}`,select:'id'});return a.length}
async function stateRows(id:string,sessionId:string){return rest('vehicle_evidence',{vehicle_id:`eq.${id}`,stage:'eq.airport_pickup',evidence_type:'eq.state_photo',event_id:'is.null','metadata->>flow_session_id':`eq.${sessionId}`,select:'id,metadata,created_at',order:'created_at.asc'})}function slotOf(e:any){return String(e?.metadata?.photo_slot||'')}async function stateSummary(id:string,sessionId:string){const rows=await stateRows(id,sessionId),present=new Set<string>(),extras:any[]=[];for(const e of rows){const s=slotOf(e);if(REQUIRED_SET.has(s))present.add(s);else extras.push(e)}const missing=REQUIRED_STATE_SLOTS.filter(s=>!present.has(s));return{total:rows.length,required_complete:REQUIRED_STATE_SLOTS.length-missing.length,required_total:REQUIRED_STATE_SLOTS.length,missing,extra_count:extras.length,max_extra:MAX_EXTRA_STATE_PHOTOS}}
function b64bytes(s:string){const raw=atob(s),a=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)a[i]=raw.charCodeAt(i);return a}function bytesB64(a:Uint8Array){let s='';for(let i=0;i<a.length;i+=0x8000)s+=String.fromCharCode(...a.subarray(i,i+0x8000));return btoa(s)}async function ocr(bytes:Uint8Array){if(!GOOGLE_VISION_KEY)return{detected:null,raw:null,error:'provider_not_configured'};const r=await fetch(`https://vision.googleapis.com/v1/images:annotate?key=${encodeURIComponent(GOOGLE_VISION_KEY)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({requests:[{image:{content:bytesB64(bytes)},features:[{type:'TEXT_DETECTION',maxResults:20}]}]})});if(!r.ok)return{detected:null,raw:null,error:`vision_${r.status}`};const j=await r.json(),raw=String(j?.responses?.[0]?.fullTextAnnotation?.text??j?.responses?.[0]?.textAnnotations?.[0]?.description??''),c=(raw.toUpperCase().match(/[A-Z0-9][A-Z0-9\-\s]{3,11}[A-Z0-9]/g)??[]).map(norm).filter((x:string)=>x.length>=4&&x.length<=10);return{detected:c.sort((a:string,b:string)=>b.length-a.length)[0]??null,raw,error:null}}
async function signed(path:string){const r=await fetch(`${SUPABASE_URL}/storage/v1/object/sign/vehicle-evidence/${path}`,{method:'POST',headers:hdr({'Content-Type':'application/json'}),body:JSON.stringify({expiresIn:3600})});if(!r.ok)return null;const j=await r.json();const p=String(j?.signedURL??j?.signedUrl??'');return p?p.startsWith('http')?p:`${SUPABASE_URL}/storage/v1${p}`:null}async function listEvidence(vehicleId:string,sessionId:string){const rows=await rest('vehicle_evidence',{vehicle_id:`eq.${vehicleId}`,stage:'eq.airport_pickup',evidence_type:'in.(state_photo,documentation)',event_id:'is.null','metadata->>flow_session_id':`eq.${sessionId}`,select:'id,evidence_type,media_type,mime_type,storage_path,created_at,event_id,metadata',order:'created_at.asc'});return Promise.all(rows.map(async(e:any)=>({...e,photo_slot:e?.metadata?.photo_slot||null,url:await signed(e.storage_path),deletable:e.event_id==null&&(e.evidence_type==='state_photo'||e.evidence_type==='documentation')})))}
async function saveEvidence(vehicleId:string,wid:string,type:string,raw:string,mime:string,mediaType:string,metadata:any={}){const bytes=b64bytes(raw.replace(/^data:[^;]+;base64,/,''));if(bytes.length<100||bytes.length>10_000_000)throw new Error('invalid_file_size');const ext=mime==='application/pdf'?'pdf':mime==='image/png'?'png':mime==='image/webp'?'webp':'jpg',path=`${vehicleId}/airport_pickup/${Date.now()}-${crypto.randomUUID()}.${ext}`;const sr=await fetch(`${SUPABASE_URL}/storage/v1/object/vehicle-evidence/${path}`,{method:'POST',headers:hdr({'Content-Type':mime,'x-upsert':'false'}),body:bytes});if(!sr.ok)throw new Error(await sr.text());const ev=await insert('vehicle_evidence',{vehicle_id:vehicleId,uploaded_by:wid,stage:'airport_pickup',evidence_type:type,media_type:mediaType,storage_path:path,mime_type:mime,file_size:bytes.length,metadata:{source:'modern_pickup',...metadata}});return{ev,bytes}}
Deno.serve(async reqq=>{const requestUrl=new URL(reqq.url);if(reqq.method==='GET'&&requestUrl.searchParams.get("attest")==="1")return releaseAttestation();if(reqq.method==='OPTIONS')return new Response(null,{status:204,headers:cors()});if(reqq.method!=='POST')return json({ok:false,error:'method_not_allowed'},405);try{const origin=reqq.headers.get('Origin');if(origin&&origin!==ORIGIN)return json({ok:false,error:'origin_not_allowed'},403);const b=await reqq.json(),uid=await auth(String(b.initData||'')),{w}=await ctx(uid),action=String(b.action||'');if(action==='resume'){const id=String(b.flow_session_id||'');const ss=await one('operation_flow_sessions',{id:`eq.${id}`,worker_id:`eq.${w.id}`,telegram_user_id:`eq.${uid}`,flow_type:'eq.pickup',select:'id,normalized_plate,vehicle_id,status,expires_at'});if(!ss)throw new Error('flow_session_not_found');if(ss.status!=='active')throw new Error('flow_session_not_active');if(new Date(ss.expires_at).getTime()<=Date.now())throw new Error('flow_session_expired');const v=await one('vehicles',{id:`eq.${ss.vehicle_id}`,select:'id,plate,status'});if(!v||norm(v.plate)!==ss.normalized_plate)throw new Error('flow_session_context_mismatch');const vr=await one('plate_verifications',{vehicle_id:`eq.${v.id}`,stage:'eq.airport_pickup','metadata->>flow_session_id':`eq.${ss.id}`,select:'id,detected_plate,result',order:'created_at.desc'});return json({ok:true,plate:v.plate,vehicle_id:v.id,flow_session_id:ss.id,requirements:{state_photo:await req('state_photo'),plate_photo:await req('plate_photo'),documentation:await req('documentation')},state:await stateSummary(v.id,ss.id),evidence:await listEvidence(v.id,ss.id),counts:{documentation:await count(v.id,'documentation',ss.id),plate_photo:await count(v.id,'plate_photo',ss.id)},verification:vr?{id:vr.id,detected_plate:vr.detected_plate,result:vr.result}:null})}if(action==='cancel_session'){const id=String(b.flow_session_id||'');if(!id)return json({ok:true,cancelled:false});const d=await rpc('cancel_operation_flow_session',{p_session_id:id,p_worker_id:w.id,p_telegram_user_id:uid,p_flow_type:'pickup'});return json(d)}if(action==='start'){const plate=norm(String(b.plate||''));if(plate.length<4||plate.length>12)return json({ok:false,error:'invalid_plate'},400);let v=await vehicle(plate),now=new Date().toISOString(),created=false;if(v)await patch('vehicles',{id:`eq.${v.id}`},{plate,status:'requested',retrieved_at:null,last_updated_by:w.id,updated_at:now});else{v=await insert('vehicles',{plate,status:'requested',last_updated_by:w.id});created=true}const s=await createPickupSession(plate,v,w,uid,created);return json({ok:true,plate,vehicle_id:v.id,flow_session_id:s.id,requirements:{state_photo:await req('state_photo'),plate_photo:await req('plate_photo'),documentation:await req('documentation')},state:await stateSummary(v.id,s.id),evidence:await listEvidence(v.id,s.id)})}
if(action==='evidence'){const plate=norm(String(b.plate||'')),v=await vehicle(plate);if(!v)return json({ok:false,error:'vehicle_not_found'},404);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);return json({ok:true,evidence:await listEvidence(v.id,s.id),state:await stateSummary(v.id,s.id),counts:{state_photo:await count(v.id,'state_photo',s.id),documentation:await count(v.id,'documentation',s.id)}})}
if(action==='delete_evidence'){const plate=norm(String(b.plate||'')),v=await vehicle(plate),id=String(b.evidence_id||'');if(!v||!id)return json({ok:false,error:'invalid_evidence'},400);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const ev=await one('vehicle_evidence',{id:`eq.${id}`,vehicle_id:`eq.${v.id}`,stage:'eq.airport_pickup','metadata->>flow_session_id':`eq.${s.id}`,select:'id,evidence_type,storage_path,event_id'});if(!ev)return json({ok:false,error:'evidence_not_found'},404);if(ev.event_id!=null||!['state_photo','documentation'].includes(ev.evidence_type))return json({ok:false,error:'evidence_not_deletable'},409);const sr=await fetch(`${SUPABASE_URL}/storage/v1/object/vehicle-evidence/${ev.storage_path}`,{method:'DELETE',headers:hdr()});if(!sr.ok&&sr.status!==404)throw new Error(await sr.text());await del('vehicle_evidence',{id:`eq.${ev.id}`});return json({ok:true,evidence:await listEvidence(v.id,s.id),state:await stateSummary(v.id,s.id),counts:{state_photo:await count(v.id,'state_photo',s.id),documentation:await count(v.id,'documentation',s.id)}})}
if(action==='state_photo'){const plate=norm(String(b.plate||'')),v=await vehicle(plate);if(!v)return json({ok:false,error:'vehicle_not_found'},404);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const slot=String(b.photo_slot||'').trim();if(!REQUIRED_SET.has(slot)&&slot!=='extra')return json({ok:false,error:'invalid_photo_slot'},400);const rows=await stateRows(v.id,s.id);if(REQUIRED_SET.has(slot)&&rows.some((e:any)=>slotOf(e)===slot))return json({ok:false,error:'photo_slot_already_filled'},409);const summary=await stateSummary(v.id,s.id);if(slot==='extra'&&summary.extra_count>=MAX_EXTRA_STATE_PHOTOS)return json({ok:false,error:'max_extra_photos_reached'},409);const r=await req('state_photo'),c=rows.length;if(r.max!=null&&c>=r.max)return json({ok:false,error:'max_state_photos_reached'},409);const saved=await saveEvidence(v.id,w.id,'state_photo',String(b.image_base64||''),'image/jpeg','photo',{photo_slot:slot,flow_session_id:s.id});return json({ok:true,count:await count(v.id,'state_photo',s.id),required:r.min,state:await stateSummary(v.id,s.id),evidence:{id:saved.ev.id,evidence_type:'state_photo',media_type:'photo',mime_type:'image/jpeg',photo_slot:slot,url:await signed(saved.ev.storage_path),deletable:true}})}
if(action==='plate_photo'){const plate=norm(String(b.plate||'')),v=await vehicle(plate);if(!v)return json({ok:false,error:'vehicle_not_found'},404);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const saved=await saveEvidence(v.id,w.id,'plate_photo',String(b.image_base64||''),'image/jpeg','photo',{flow_session_id:s.id}),o=await ocr(saved.bytes),det=o.detected?norm(o.detected):null,matched=!!det&&det===plate,vr=await insert('plate_verifications',{vehicle_id:v.id,worker_id:w.id,evidence_id:saved.ev.id,stage:'airport_pickup',expected_plate:plate,detected_plate:det,ocr_raw_text:o.raw,result:matched?'matched':(det?'mismatch':'ocr_failed'),metadata:{ocr_provider:GOOGLE_VISION_KEY?'google_vision':'unconfigured',ocr_error:o.error,source:'modern_pickup',flow_session_id:s.id}});return json({ok:true,matched,detected_plate:det,verification_id:vr.id})}
if(action==='override'){const plate=norm(String(b.plate||'')),v=await vehicle(plate),id=String(b.verification_id||'');if(!v||!id)return json({ok:false,error:'invalid_override'},400);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const vr=await one('plate_verifications',{id:`eq.${id}`,vehicle_id:`eq.${v.id}`,select:'id,metadata'});if(!vr)return json({ok:false,error:'verification_not_found'},404);if(String(vr.metadata?.flow_session_id||'')!==s.id)return json({ok:false,error:'verification_session_mismatch'},409);await patch('plate_verifications',{id:`eq.${id}`},{result:'overridden',override_reason:'operator_override',worker_id:w.id,metadata:{...(vr.metadata||{}),override_by_telegram_user_id:uid,overridden_at:new Date().toISOString(),source:'modern_pickup'}});return json({ok:true})}
if(action==='documentation'){const plate=norm(String(b.plate||'')),v=await vehicle(plate);if(!v)return json({ok:false,error:'vehicle_not_found'},404);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const mime=String(b.mime_type||'application/octet-stream'),media=mime.startsWith('image/')?'photo':'document',r=await req('documentation'),c=await count(v.id,'documentation',s.id);if(r.max!=null&&c>=r.max)return json({ok:false,error:'max_documents_reached'},409);const saved=await saveEvidence(v.id,w.id,'documentation',String(b.file_base64||''),mime,media,{flow_session_id:s.id});return json({ok:true,count:await count(v.id,'documentation',s.id),required:r.min,evidence:{id:saved.ev.id,evidence_type:'documentation',media_type:media,mime_type:mime,url:await signed(saved.ev.storage_path),deletable:true}})}
if(action==='status'){const plate=norm(String(b.plate||'')),v=await vehicle(plate);if(!v)return json({ok:false,error:'vehicle_not_found'},404);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const rs=await req('state_photo'),rp=await req('plate_photo'),rd=await req('documentation'),ss=await stateSummary(v.id,s.id),cp=await count(v.id,'plate_photo',s.id),cd=await count(v.id,'documentation',s.id);return json({ok:true,counts:{state_photo:ss.total,plate_photo:cp,documentation:cd},state:ss,requirements:{state_photo:rs,plate_photo:rp,documentation:rd},ready:ss.missing.length===0&&cp>=rp.min&&cd>=rd.min,evidence:await listEvidence(v.id,s.id)})}
if(action==='finish'){const plate=norm(String(b.plate||'')),v=await vehicle(plate);if(!v)return json({ok:false,error:'vehicle_not_found'},404);const s=await requirePickupSession(String(b.flow_session_id||''),plate,v,w,uid);const rp=await req('plate_photo'),rd=await req('documentation'),ss=await stateSummary(v.id,s.id),cp=await count(v.id,'plate_photo',s.id),cd=await count(v.id,'documentation',s.id);if(ss.missing.length||cp<rp.min||cd<rd.min)return json({ok:false,error:'missing_evidence',state:ss,counts:{state_photo:ss.total,plate_photo:cp,documentation:cd}},409);const verificationId=String(b.verification_id||'');if(!verificationId)return json({ok:false,error:'verification_required'},409);const d=await rpc('complete_pickup_flow',{p_session_id:s.id,p_worker_id:w.id,p_telegram_user_id:uid,p_plate:plate,p_verification_id:verificationId});return json(d)}return json({ok:false,error:'invalid_action'},400)}catch(e){console.error(e);const m=String((e as Error)?.message||e);return json({ok:false,error:m},m==='not_authorized'?403:400)}});