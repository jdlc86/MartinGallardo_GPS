(function(){
  if(window.__PMG_RESERVATION_OPERATIONAL__)return;
  window.__PMG_RESERVATION_OPERATIONAL__=true;
  var API='https://mvexykcxnpaywkbnoxwu.supabase.co/functions/v1/reservation-operational-api';
  var last=null,timer=null,busy=false;
  function norm(v){return String(v||'').toUpperCase().replace(/[^A-Z0-9]/g,'')}
  function css(){if(document.getElementById('pmg-op-style'))return;var s=document.createElement('style');s.id='pmg-op-style';s.textContent='.op-status{display:inline-flex!important;width:auto!important;margin-top:7px!important;padding:5px 8px;border-radius:999px;font-size:9px!important;font-weight:950;letter-spacing:.055em;line-height:1;white-space:nowrap}.op-PENDIENTE{background:var(--pmg-warning-soft);color:var(--pmg-warning-text)}.op-RECOGIDO{background:var(--pmg-info-soft);color:var(--pmg-info-text)}.op-APARCADO{background:var(--pmg-success-soft);color:var(--pmg-success-text)}.op-REUBICADO{background:color-mix(in srgb,var(--pmg-accent) 16%,transparent);color:var(--pmg-accent-text)}.op-ENTREGADO{background:var(--pmg-soft);color:var(--pmg-muted)}';document.head.appendChild(s)}
  function labels(){var a=document.getElementById('statActive'),t=document.getElementById('statToday'),u=document.getElementById('statUpcoming');if(a&&a.parentElement){var x=a.parentElement.querySelector('span');if(x)x.textContent='Entregados hoy'}if(t&&t.parentElement){var y=t.parentElement.querySelector('span');if(y)y.textContent='Recogidas hoy'}if(u&&u.parentElement){var z=u.parentElement.querySelector('span');if(z)z.textContent='Próximas'}}
  function apply(){if(!last)return;css();labels();var stats=last.stats||{},a=document.getElementById('statActive'),t=document.getElementById('statToday'),u=document.getElementById('statUpcoming');if(a)a.textContent=Number(stats.delivered_today||0);if(t)t.textContent=Number(stats.pickups_today||0);if(u)u.textContent=Number(stats.upcoming||0);var map={};(last.statuses||[]).forEach(function(x){map[norm(x.plate)]=x});document.querySelectorAll('.booking').forEach(function(card){var plate=card.querySelector('.vehicle-cell .plate');if(!plate)return;var key=norm(plate.textContent),st=map[key]||{status:'PENDIENTE'},cell=card.querySelector('.vehicle-cell');if(!cell)return;var badge=cell.querySelector('.op-status');if(!badge){badge=document.createElement('span');badge.className='op-status';cell.appendChild(badge)}badge.className='op-status op-'+st.status;badge.textContent=st.status;badge.title=st.at?'Estado operativo actualizado '+new Date(st.at).toLocaleString('es-ES'):st.status==='PENDIENTE'?'Sin manipulación operativa registrada':''})}
  async function refresh(){if(busy||document.hidden)return;var tg=window.Telegram&&window.Telegram.WebApp;if(!tg||!tg.initData)return;busy=true;try{var r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({initData:tg.initData})}),d=await r.json().catch(function(){return null});if(r.ok&&d&&d.ok){last=d;apply()}}catch(e){}finally{busy=false}}
  function start(){if(!/\/reservations-admin\.html$/i.test(location.pathname))return;css();labels();var list=document.getElementById('bookingList');if(list)new MutationObserver(function(){apply()}).observe(list,{childList:true,subtree:true});refresh();timer=setInterval(refresh,5000);document.addEventListener('visibilitychange',function(){if(!document.hidden)refresh()});window.addEventListener('pagehide',function(){if(timer)clearInterval(timer)})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();
